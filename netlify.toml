[build]
  command = "npm run build"
  publish = "build"

# SPA: todas las rutas van a index.html (excepto archivos estáticos)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
